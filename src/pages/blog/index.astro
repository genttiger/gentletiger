---
const posts = Object.values(import.meta.glob('../../content/blog/*.md', { 
  eager: true, 
  import: 'frontmatter' 
}));
---

<h1>Blog</h1>

<ul>
{
posts.map((post) => (
<li>
<h2>{post.title}</h2>
<p>{post.description}</p>
</li>
))
}
</ul>
